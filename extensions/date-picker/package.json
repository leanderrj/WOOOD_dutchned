{
    "name": "date-picker",
    "version": "1.0.0",
    "license": "UNLICENSED",
    "scripts": {
        "shopify": "npm exec -- shopify",
        "build": "tsc && npm exec -- shopify app build",
        "dev": "npm exec -- shopify app dev",
        "preview": "npm exec -- shopify app function run",
        "type-check": "tsc --noEmit",
        "clean": "rm -rf dist"
    },
    "dependencies": {
        "@shopify/ui-extensions": "^2024.10.0",
        "@shopify/ui-extensions-react": "^2024.10.0",
        "@tanstack/react-query": "^5.17.0",
        "react": "^18.2.0"
    },
    "devDependencies": {
        "@types/react": "^18.2.0",
        "typescript": "^5.0.0"
    }
}